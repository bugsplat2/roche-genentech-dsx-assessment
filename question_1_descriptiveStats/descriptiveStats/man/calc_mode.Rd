% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/descriptive_stats.R
\name{calc_mode}
\alias{calc_mode}
\title{Calculate mode}
\usage{
calc_mode(x, na_rm = FALSE)
}
\arguments{
\item{x}{A vector (numeric, character, factor).}

\item{na_rm}{Logical; if TRUE, remove NA values before computation.}
}
\value{
A vector of modes, or NA if there is no mode, or NA if empty after NA handling.
}
\description{
Calculates the statistical mode (most frequent value) of a vector.
Handles ties and "no mode" cases.
}
\details{
\strong{Tie handling:} if multiple values share the maximum frequency, returns a vector
of all modes (sorted).

\strong{No mode handling:} if all values occur with equal frequency (e.g., all unique),
returns NA.
}
\examples{
calc_mode(c(1, 2, 2, 3))
calc_mode(c(1, 2, 3))  # no mode -> NA
calc_mode(c("a", "b", "b", "c"))
}
